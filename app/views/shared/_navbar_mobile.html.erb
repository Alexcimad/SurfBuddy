    <nav class="nav--icons">
      <ul>
        <li>
          <%= link_to root_path do %>
            <div class="nav-i"><%= icon('fa','home') %></div>
            <span>Home</span>
          <% end %>
        </li>
        <li>
          <%= link_to surf_spots_path do %>
            <div class="nav-i"><%= icon('fa','map-marked-alt') %></div>
            <span>Map</span>
          <% end %>
        </li>

        <%# <li>
          <a data-toggle="modal" data-target="#exampleModal" class="link">
          <div class="nav-i"><%= icon('fa','search') </div>%>
            <%# <span>Search</span> %>
          <%# </a> %>
          <%# <%= render 'shared/search_bar_modal'%>
        <%# </li> %>

        <% if user_signed_in? %>
        <li>
          <%= link_to profile_path do %>
            <div class="nav-i"><%= icon('fa','user-alt') %></div>
            <span>Profile</span>
            <% end %>
        </li>
        <% else %>
        <li>
          <%= link_to new_user_session_path do %>
            <div class="nav-i"><%= icon('fa','user-alt') %></div>
            <span>Login</span>
            <% end %>
        </li>
        <% end %>

      </ul>
    </nav>
